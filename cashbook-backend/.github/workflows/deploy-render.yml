name: Deploy to Render

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: |
          echo "Push detected. Render will auto-deploy from GitHub."
          echo "Make sure your Render service is connected to this GitHub repository."

